#ifndef PATH_RESOLVER_H
#define PATH_RESOLVER_H

// Automatically adjusts the current working directory so the engine
// can safely find the assets/ and shaders/ folders
void SetupWorkingDirectory();

#endif